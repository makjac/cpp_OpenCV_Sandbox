#include "colorpicker.h"

void Colorpicker::setPoint(int x, int y)
{
    point.x = x;
    point.y = y;
    /*h=(int)img.at<cv::Vec3b>(y, x)[0];
    s=(int)img.at<cv::Vec3b>(y, x)[1];
    v=(int)img.at<cv::Vec3b>(y, x)[2];*/
}